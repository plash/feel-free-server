projects:
  app:
    schemaPath: config/schema.graphql
    extensions:
      endpoints:
        default: http://localhost:4000
  database:
    schemaPath: config/generated/prisma.graphql
    extensions:
      prisma: database/prisma.yml